.row
  .medium-12.columns
    %ul.breadcrumbs
      %li
        = link_to t("home"), "/"
      %li
        = link_to Conference.friendly.find(params[:conference_id]).title, :controller => 'conferences', :action => 'show', :id => params[:conference_id]
      %li.current
        = t("event.new_event")
.row
  .medium-12.columns
    %h1
      = t("thanks.vote_thanks")
    %p 
      = t("thanks.vote_msg")
    %p
      = t("thanks.thanks_msg")
      = mail_to "info@gul.es", "info@gul.es", subject: "[GUL TALKS]"
.row
  .large-6.columns
    = link_to t("event.back_to_events"), "/", :class => "button radius expand"
