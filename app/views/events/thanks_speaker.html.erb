<% page_title t("views.speaker.propose") %>

<%= event_breadcrumbs(@conference, @event, "speaker") %>

<%= render partial: "partials/view_header", locals: { header: "thanks.speaker_thanks" } %>

<div class="pure-g">
    <div class="pure-u-1 pure-u-lg-1-2">
        <p><%= t("views.thanks.speaker_thanks_msg") %></p>
    </div>
</div>

<div class="pure-g">
    <div class="pure-u-1 pure-u-lg-1-2">
        <%= link_to t("views.event.back_to_event"), conference_event_path(@conference, @event), class: "button-primary radius full-width" %>
    </div>
</div>
