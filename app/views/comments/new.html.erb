<% page_title t("views.event.comment.send_comment") %>

<div class="pure-g">
    <div class="pure-u-1 pure-u-lg-1-2">
        <h3 class="subheader">
            <%= t("views.event.comment.replying_to") %>
            <%= h(@parent.name) %>
        </h3>
    </div>
</div>

<div class="pure-g">
    <div class="pure-u-1 pure-u-lg-1-2">
        <i><%= simple_format(h(@parent.content)) %></i>
    </div>
</div>

<%= render 'form' %>
