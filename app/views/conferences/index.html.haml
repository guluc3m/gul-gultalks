.row
  .medium-7.columns
    %ul.breadcrumbs
      %li
        = link_to t("home"), root_path
      %li.current
        = t("conference.list")
  .large-12.columns
    .medium-5.columns
      %h1.subheader
        = t("Conferences")
    %hr

    .large-12.columns
      .large-block-grid-4
        - @conferences.each do |conference|
          - unless conference.active
            .panel.radius.expand
              %h4
                = link_to conference.title, conference_path(conference)
                / = l conference.start_date, :format => :year

/ %table
/   %thead
/     %tr
/       %th{:width => "960"}
/         %i.fa.fa-calendar
/         = t("Conference")
/   %tbody
/     - @conferences.each do |conference|
/       %tr
/         %td
/           = l conference.start_date, :format => :year
/           %span

/           = link_to conference.title, :controller => 'conferences', :action => 'show', :id => conference
/           %td
/             = l conference.start_date, :format => :month_year
