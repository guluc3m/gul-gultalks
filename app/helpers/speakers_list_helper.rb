module SpeakersListHelper
  
  def get_twitter_pic(userId = nil)
    
  end
  
  
  
  def list_spakers(spakers)
    #pakers.speaker_list.each_with_index.map do |speaker, index|
    # = h(speaker.name)
    # - if speaker.twitter.present?
    #   = "("
    #   = link_to h("@#{speaker.twitter}"), "https://twitter.com/#{speaker.twitter}", target: "_blank"
    #   = ")"
    # - unless index+1 == @event.speaker_list.length
    #   = ", "
    
  end
  
end
